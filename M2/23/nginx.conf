events {
    worker_connections 1024;
}
http {
    server {
        include /etc/nginx/mime.types;

        listen       80;
        root         /usr/share/nginx/html;
        location / {

        }
    }
}

